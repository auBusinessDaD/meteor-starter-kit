mutation addComment($studentId: String!, $comment: String!) {
  addComment(studentId: $studentId, comment: $comment) {
    _id
    studentId
    comment
    createdAt
    user {
      _id
      name {
        first
        last
      }
    }
  }
}
